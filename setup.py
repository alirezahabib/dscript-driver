from setuptools import setup

setup(
    name='dscript-driver',
    version='0.1.0',
    author='LPQM',
    description='A Python driver for the dScript based switches',
    packages=['dscript'],
    install_requires=[],
)
